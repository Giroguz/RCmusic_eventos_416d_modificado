PORT=8787

# PACK TODOS LOS GÉNEROS staging project only. Never point this service at RCmusic_eventos.
# This API uses only the isolated project's publishable/anon key, never a service-role key.
SUPABASE_URL=https://uwvhpodseawpsqunplld.supabase.co
SUPABASE_ANON_KEY=

# Optional secret JSON for the PACK DJ Drive service account. Keep it only in the staging host's secret store.
# The API requests the Drive readonly OAuth scope. Grant this identity Reader on the PACK folder only.
# Do not grant Writer/Editor; DJ permission-management endpoints remain disabled in this mode.
GOOGLE_SERVICE_ACCOUNT_JSON=
DRIVE_FOLDER_ID=1UTIQESYvJcNdKXNsDdDs0dRCrDzs5JvF

# Set only after verifying the actual standalone PWA staging origin(s); comma-separated exact origins, no wildcard.
CORS_ORIGIN=
